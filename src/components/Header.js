import React from 'react';

const Header = () => { 
    return (
        <header className="App-header">
          <h1 className="App-title">Ping Pong!</h1>
          <h2 className="App-subtitle">8 player tournament generator</h2>
        </header>
    );  
}

export default Header;